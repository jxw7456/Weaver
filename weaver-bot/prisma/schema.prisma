// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Ticket {
  id          Int       @id @default(autoincrement())
  userId      String
  channelId   String    @unique
  guildId     String
  subject     String
  category    String
  status      String    @default("open")
  assignedTo  String?
  createdAt   DateTime  @default(now())
  closedAt    DateTime?
  escalatedAt DateTime?
  aiResponded Boolean   @default(false)
  
  // New tracking fields
  tracked     Boolean   @default(false)
  trackedAt   DateTime?
  trackedBy   String?
  trackingNotes String?
  notionPageId String?  // For future Notion integration
  
  messages    Message[]
  feedback    Feedback?
}

model Message {
  id        Int      @id @default(autoincrement())
  ticketId  Int
  authorId  String
  content   String
  isAI      Boolean  @default(false)
  createdAt DateTime @default(now())
  
  ticket    Ticket   @relation(fields: [ticketId], references: [id])
}

model Feedback {
  id        Int      @id @default(autoincrement())
  ticketId  Int      @unique
  rating    Int
  comment   String?
  createdAt DateTime @default(now())
  
  ticket    Ticket   @relation(fields: [ticketId], references: [id])
}

model FAQ {
  id         Int      @id @default(autoincrement())
  question   String
  answer     String   @db.Text
  category   String
  keywords   String[] // Array for search functionality
  views      Int      @default(0)
  helpful    Int      @default(0)
  notHelpful Int      @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

// New model for tracked tickets queue
model TrackedTicket {
  id          Int      @id @default(autoincrement())
  ticketId    Int      @unique
  rating      Int
  priority    String   @default("normal") // low, normal, high, urgent
  status      String   @default("pending") // pending, in_review, resolved, exported
  notes       String?
  trackedBy   String
  reviewedBy  String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  exportedAt  DateTime? // When exported to Notion
  notionPageId String?  // Notion page ID once exported
}